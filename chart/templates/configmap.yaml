apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "eb-demo-service.fullname" . }}-config
  labels:
    {{- include "eb-demo-service.labels" . | nindent 4 }}
data:
  APP_NAME: {{ .Values.app.name | quote }}
  VERSION: {{ .Values.app.version | quote }}
